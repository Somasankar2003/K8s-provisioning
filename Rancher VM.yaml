---
- name: Create OpenStack VMs and auto-register to Rancher
  hosts: localhost
  connection: local
  gather_facts: no

  vars:
    ###########################################################################
    # Rancher details
    ###########################################################################
    rancher_url: "https://10.68.0.173"
    rancher_token: "token-89chg:xv4wc4tmkxhh4t8dz6gzd84dqpghzh8rsfbn7lb5sssc57hmjfncwk"
    cluster_name: "{{ cluster_name }}"

    ###########################################################################
    # Survey variables (AAP)
    ###########################################################################
    vm_count: "{{ vm_count | int }}"
    node_roles: "{{ node_roles }}"
    vm_net: "{{ vm_net }}"
    vm_flavor: "{{ vm_flavor }}"
    vm_security_groups: "{{ vm_security_groups }}"
    vm_names_list: "{{ vm_name_prefix.split(',') | map('trim') | list }}"

    ###########################################################################
    # Root volume (FIXED)
    ###########################################################################
    root_volume_size: 40

    ###########################################################################
    # Derived variables
    ###########################################################################
    parsed_security_groups: "{{ vm_security_groups.split(',') | map('trim') | list }}"

    ###########################################################################
    # VM details
    ###########################################################################
    vm_image: "c2326965-428c-47b0-b2a8-35f81b3eeeb1"
    vm_keypair: "k8"

    ###########################################################################
    # OpenStack auth
    ###########################################################################
    os_auth_url: "https://keystone-public-openstack.apps.pgrhoso.dccloud.com"
    os_project_name: "admin"
    os_project_domain_id: "default"
    os_username: "admin"
    os_user_domain_name: "Default"
    os_password: "stl@123"
    os_region_name: "regionOne"
    os_interface: "public"
    os_identity_api_version: "3"

  tasks:
  ###########################################################################
  # VALIDATION
  ###########################################################################
  - name: Validate inputs
    assert:
      that:
        - vm_count >= 1
        - vm_names_list | length == vm_count
      fail_msg: "vm_count must match VM names"

  - name: Normalize node roles
    set_fact:
      parsed_roles: >-
        {{
          [node_roles]
          if vm_count == 1
          else (node_roles.split(',') | map('trim') | list)
        }}

  - name: Validate role count
    assert:
      that:
        - parsed_roles | length == vm_count
      fail_msg: "Role count must match vm_count"

  - name: Build VM role mapping
    set_fact:
      vm_role_map: "{{ vm_names_list | zip(parsed_roles) | list }}"

  ###########################################################################
  # GET RANCHER CLUSTER INFO
  ###########################################################################
  - name: Get cluster ID from Rancher
    uri:
      url: "{{ rancher_url }}/v3/clusters?name={{ cluster_name }}"
      method: GET
      headers:
        Authorization: "Bearer {{ rancher_token }}"
      validate_certs: no
    register: cluster_info

  - name: Fail if cluster not found
    fail:
      msg: "Cluster '{{ cluster_name }}' not found in Rancher."
    when: cluster_info.json.data | length == 0

  - set_fact:
      cluster_id: "{{ cluster_info.json.data[0].id }}"

  ###########################################################################
  # GET RANCHER JOIN TOKEN
  ###########################################################################
  - name: Get registration token
    uri:
      url: "{{ rancher_url }}/v3/clusterRegistrationTokens?clusterId={{ cluster_id }}"
      method: GET
      headers:
        Authorization: "Bearer {{ rancher_token }}"
      validate_certs: no
    register: reg_info
    until: reg_info.json.data | length > 0 and reg_info.json.data[0].nodeCommand is defined
    retries: 15
    delay: 3

  - set_fact:
      node_command: "{{ reg_info.json.data[0].nodeCommand | replace('curl ', 'curl -k ') }}"

  ###########################################################################
  # CLOUD-INIT (FIXED — DOCKER REMOVED)
  ###########################################################################
  - name: Create cloud-init files
    copy:
      dest: "/tmp/cloud-init-{{ item.0 }}.yml"
      content: |
        #cloud-config
        package_update: true
        packages:
          - curl
          - jq
          - nfs-common

        runcmd:
          - |
            CMD="{{ node_command }}"
            {% if 'etcd' in item.1 %}CMD="$CMD --etcd"{% endif %}
            {% if 'controlplane' in item.1 %}CMD="$CMD --controlplane"{% endif %}
            {% if 'worker' in item.1 %}CMD="$CMD --worker"{% endif %}
            CMD="$CMD --node-name {{ item.0 }}"
            echo "$CMD" > /root/join.sh
            chmod +x /root/join.sh
            /root/join.sh
    loop: "{{ vm_role_map }}"

  ###########################################################################
  # CREATE VMS (ROOT DISK FIXED)
  ###########################################################################
  - name: Create VM on OpenStack with 40GB root disk
    openstack.cloud.server:
      state: present
      name: "{{ item.0 }}"
      flavor: "{{ vm_flavor }}"
      key_name: "{{ vm_keypair }}"
      security_groups: "{{ parsed_security_groups }}"
      network: "{{ vm_net }}"
      userdata: "{{ lookup('file', '/tmp/cloud-init-' + item.0 + '.yml') }}"
      boot_from_volume: true
      volume_size: "{{ root_volume_size }}"
      image: "{{ vm_image }}"
      terminate_volume: true
      auto_ip: yes
      wait: yes
      timeout: 900
      validate_certs: false
    environment:
      OS_AUTH_URL: "{{ os_auth_url }}"
      OS_PROJECT_NAME: "{{ os_project_name }}"
      OS_PROJECT_DOMAIN_ID: "{{ os_project_domain_id }}"
      OS_USERNAME: "{{ os_username }}"
      OS_USER_DOMAIN_NAME: "{{ os_user_domain_name }}"
      OS_PASSWORD: "{{ os_password }}"
      OS_REGION_NAME: "{{ os_region_name }}"
      OS_INTERFACE: "{{ os_interface }}"
      OS_IDENTITY_API_VERSION: "{{ os_identity_api_version }}"
    loop: "{{ vm_role_map }}"

  ###########################################################################
  # CLEANUP
  ###########################################################################
  - name: Clean temp files
    file:
      path: "/tmp/cloud-init-{{ item }}.yml"
      state: absent
    loop: "{{ vm_names_list }}"

  - name: Success message
    debug:
      msg: |
        ✅ SUCCESS
        Cluster: {{ cluster_name }}
        Root Disk: {{ root_volume_size }}GB
        Nodes: {{ vm_names_list | join(', ') }}
